import * as React from "react";
import Typography from "@mui/material/Typography";

export default function BackupDashboard() {
    return (
        <div className="home">
            <Typography className="backup backup-title" variant="h1" component="h1">Backup Dashboard</Typography>
        </div>
    );
}