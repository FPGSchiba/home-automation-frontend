import * as React from "react";
import Typography from "@mui/material/Typography";

export default function BackupSettings() {
    return (
        <div className="home">
            <Typography className="backup backup-title" variant="h1" component="h1">Backup Settings</Typography>
        </div>
    );
}